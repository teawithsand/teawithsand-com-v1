import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.teawithsand.app',
  appName: 'tws-capacitor-frontend',
  webDir: 'public',
  bundledWebRuntime: false
};

export default config;
